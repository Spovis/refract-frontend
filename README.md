Installation

`npm install` for development or `npm ci` for production

Deployment

`npm run build` to build the production image. backend must already be running when starting the frontend. The production deployment is currently on an AWS EC2 instance.

Development

To start the frontend dev server, run `npm run dev`. 

